.margin3{:class => I18n.locale}
  %h1 لیست دسته های فعالیت ها
  - if is_admin?
    = link_to 'ایجاد دسته جدید محصولات', new_category_path, :class => "btn btn-success"
  %table
    %tr
      %th Title en
      %th Title fa
      %th Description en
      %th Description fa
      %th
      %th
      %th

    - @categories.each do |category|
      %tr
        %td= category.title_en
        %td= category.title_fa
        %td= category.description_en
        %td= category.description_fa
        %td= link_to 'Show', category
        %td= link_to 'Edit', edit_category_path(category)
        %td= link_to 'Destroy', category, :method => :delete, :data => { :confirm => 'Are you sure?' }



